{
  // Primary Xdebug workflow: PhpStorm (see api/docs/xdebug.md). This config is for VS Code / Cursor.
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Listen for Xdebug (API)",
      "type": "php",
      "request": "launch",
      "port": 9003,
      "pathMappings": {
        "/app": "${workspaceFolder}/api"
      }
    }
  ]
}
