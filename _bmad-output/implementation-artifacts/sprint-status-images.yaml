# generated: 2026-08-24
# last_updated: 2026-08-27
# project: ERPify
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# SCOPED SPRINT FILE — NOT THE FULL PROJECT TRACKER:
# ====================================================
# This file tracks ONLY the Shared/Images epic (branch docs/shared-images-epics-q66r,
# worktree .claude/worktrees/shared-images-epics-q66r). It deliberately excludes every
# already-`done` epic that sprint-status.yaml on this branch inherited from `main` at
# branch-point (E1-E3 audit trail, auth-foundation, RBAC model, identity-invitation
# lifecycle, users-admin, gdpr-hardening, backlog-resolution) — that history is not
# repeated here so this board stays a clean, single-purpose view of the active work.
# It is a SEPARATE file from sprint-status.yaml on purpose: it is not the canonical
# tracker any BMAD tooling (bmad-dev-story, bmad-dev-auto, `make bmad.status.audit`)
# resolves against by default, and it must not be confused with it when this branch
# merges. When Shared/Images ships, fold `epic-images` and its stories into the
# canonical sprint-status.yaml (on `main`, in the primary checkout) as new entries
# appended after epic-backlog-resolution — do not replace that file's history with
# this one's.

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-08-24
last_updated: 2026-08-27
project: ERPify
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic images (Shared/Images): seam compartido de subida y servido de representaciones
  # fungibles (logos, avatares, imágenes de producto, miniaturas) — docs/adr/images-vs-
  # documents-conservation-contract.md (D6) + _bmad-output/planning-artifacts/epics-images.md.
  # Primera rebanada: sin cablear consumidor real (Bank.logoImageId / User.avatarImageId no
  # existen aún) y sin auditar la ruta de lectura — ambas decisiones confirmadas con Sergio,
  # quedan para una épica de consumidor aparte. Distinto del contexto Documents (evidencia,
  # issue #268), fuera de esta épica. Requisitos revisados adversarialmente (2026-08-23): 30
  # hallazgos triados, uno rechazado con cita del ADR, el resto incorporado.
  # Story 1.1 ya tiene fichero propio
  # (img-1-1-subir-imagen-obtener-representacion-canonica.md: bmad-create-story, una vuelta de
  # revisión externa y las decisiones de canonicalización cerradas con Sergio el 2026-08-27) —
  # por eso pasa a ready-for-dev y la épica a in-progress. Stories 1.2 y 1.3 siguen en backlog
  # (solo cortadas dentro de epics-images.md, sin fichero propio todavía).
  # img-1-1: code review (bmad-code-review, PR #862) completo 2026-08-27 — 1 decision-needed
  # resuelto (fail-closed) y 6 patch aplicados, make php.stan/php.quality/php.unit en verde.
  epic-images: in-progress
  img-1-1-subir-imagen-obtener-representacion-canonica: done
  img-1-2-persistir-imagen-borrado-fiable-de-bytes: ready-for-dev
  img-1-3-leer-representacion-canonica-de-forma-segura: backlog
  epic-images-retrospective: optional
