# generated: 2026-08-24
# last_updated: 2026-08-31
# project: ERPify
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# SCOPED SPRINT FILE — NOT THE FULL PROJECT TRACKER:
# ====================================================
# This file tracks ONLY the Shared/Images epic (branch docs/shared-images-epics-q66r,
# worktree .claude/worktrees/shared-images-epics-q66r). It deliberately excludes every
# already-`done` epic that sprint-status.yaml on this branch inherited from `main` at
# branch-point (E1-E3 audit trail, auth-foundation, RBAC model, identity-invitation
# lifecycle, users-admin, gdpr-hardening, backlog-resolution) — that history is not
# repeated here so this board stays a clean, single-purpose view of the active work.
# It is a SEPARATE file from sprint-status.yaml on purpose: `bmad-dev-story` and
# `bmad-dev-auto` do not resolve against it, and it must not be confused with the
# canonical tracker. `make bmad.status.audit` is no longer in that list — it globs
# `sprint-status-*.y*ml` and reads this board too, a change this epic's own
# retrospective forced after measuring check A blind to the epic for its whole life.
#
# THE FOLD IS DONE. `epic-images` and its three stories are appended after
# epic-backlog-resolution in the canonical sprint-status.yaml.
#
# THE FIVE KEYS ARE DELIBERATELY IN BOTH FILES, AND THE CANONICAL ONE IS AUTHORITATIVE.
# Saying "neither is a copy of the other" was simply false — both carry the same five
# `development_status` keys with the same values, and nothing reconciles them. What
# keeps that from rotting is that this board is FROZEN at the shipped state: the epic
# is done, every key here is `done`, and no marker moves again. Should one ever need
# to, move it in the canonical file; the audit now reads both, so a divergence surfaces
# as a duplicated report rather than silently. The prose above and below the keys is
# what this file is for now — the epic's own history. The ~4400-line planning record
# is NOT here: it is the three `img-1-*.md` artifacts (895 + 1606 + 1894 lines).

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-08-24
last_updated: 2026-08-31
project: ERPify
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic images (Shared/Images): seam compartido de subida y servido de representaciones
  # fungibles (logos, avatares, imágenes de producto, miniaturas) — docs/adr/images-vs-
  # documents-conservation-contract.md (D6) + _bmad-output/planning-artifacts/epics-images.md.
  # Primera rebanada: sin cablear consumidor real (Bank.logoImageId / User.avatarImageId no
  # existen aún) y sin auditar la ruta de lectura — ambas decisiones confirmadas con Sergio,
  # quedan para una épica de consumidor aparte. Distinto del contexto Documents (evidencia,
  # issue #268), fuera de esta épica. Requisitos revisados adversarialmente (2026-08-23): 30
  # hallazgos triados, uno rechazado con cita del ADR, el resto incorporado.
  # Story 1.1 ya tiene fichero propio
  # (img-1-1-subir-imagen-obtener-representacion-canonica.md: bmad-create-story, una vuelta de
  # revisión externa y las decisiones de canonicalización cerradas con Sergio el 2026-08-27) —
  # por eso pasa a ready-for-dev y la épica a in-progress.
  # img-1-2: implementada y en review desde 2026-08-29. Tres pases adversariales registrados en su
  # sección `## Adversarial pass`: dos sobre el artefacto (2026-08-28, 16 patches, entre ellos el
  # enrutado de la señal de borrado) y uno sobre el código en tres lecturas paralelas (2026-08-29),
  # que encontró dos GRAVE con todas las puertas en verde — la existencia indecidible un nivel por
  # encima del directorio guardado, y la raíz de almacenamiento que ningún despliegue creaba.
  # img-1-3: ya tiene fichero propio
  # (img-1-3-leer-representacion-canonica-de-forma-segura.md: bmad-create-story, 2026-08-29) — por eso pasa a
  # ready-for-dev. Su análisis midió el árbol en 202767ab y encontró catorce desajustes con el corte del
  # épico, cuatro capaces de dejar una decisión del épico falsa con todas las puertas en verde: la auditoría
  # genérica se activa por defecto sobre todo GET 2xx bajo /api/ y sólo se desactiva por el NOMBRE de la ruta
  # (`str_starts_with($route, 'shared_')`, AuditPolicy.php:66) — rama que hoy no usa ninguna ruta y no pincha
  # ningún test, así que la decisión «cero filas de audit_log» dependía de un acoplamiento invisible; la
  # opción «nivel debug» que el épico ofrece como una de las tres cotas del log de ausencia está descartada
  # por ObservabilityChannelGateTest:42,85, que fija `info` en test y prod; ningún recurso de
  # api/config/routes.yaml carga src/Shared/, así que un #[Route] ahí no registra nada y todo queda verde; y
  # el path `GET /images/{imageId}` que escribe el épico cae fuera del `^/api` del firewall sin que
  # php.lint.public-access lo note. Corregido además RESIDUAL-1, que describía como inexistente el limitador
  # global anonymous_api (120 req/min por IP, aplicado sin mirar la autenticación). Queda una decisión abierta
  # para Sergio antes de implementar (Task 0).
  # img-1-3: pase adversarial EXTERNO completado 2026-08-30 en tres lecturas paralelas sobre el artefacto ya
  # commiteado. CAMBIÓ el resultado, no lo confirmó: 34 hallazgos, CERO rechazados, seis GRAVE que tumbaron
  # cuatro AC y la premisa entera de Task 0. (1) El brazo 503 de la AC 6 era mecánicamente imposible —
  # ProblemDetailsFactory sólo lee marcadores dentro del brazo `instanceof DomainException` de su match
  # (:266/:351-357/:494), así que marcar una clase del puerto habría dado 500, el mismo resultado que el brazo
  # permanente que la AC decía no conflacionar. (2) La AC 16 emparejaba dos tests sobre resolvers DISJUNTOS de
  # Symfony (MapUploadedFile no pasa por el serializador), de modo que el segundo pasaba con el guard borrado
  # — y el test correcto YA EXISTÍA en el árbol desde antes de la épica. (3) El literal de Cache-Control de la
  # AC 11 no es lo que sale por el cable: HeaderBag hace ksort y AbstractSessionListener añade must-revalidate
  # y Expires. (4) El caso de integridad de la AC 8 ponía rojo StorageFailureVocabularyTest y contradecía el
  # docblock del propio enum, sin emisor posible donde Task 5 lo ponía. (5) Task 0 colgaba de una premisa
  # FALSA heredada del épico y del defer de img-1-2 y nunca medida: bajo el orden findById → read que la
  # propia historia manda, un id aleatorio no toca storage y emite cero líneas; los productores reales son la
  # raíz inusable y el fallo transitorio, más ruidosos y con flush del fingers_crossed. (6) Ninguna fixture
  # puede sembrar una Image con bytes, así que siete filas de la matriz eran inconstruibles. Además: CORS
  # bloquea el cache condicional que la historia entrega (AC 24 nueva), las cabeceras del propio 304 no
  # estaban especificadas, y se corrigieron una CITA FABRICADA atribuida a api/CLAUDE.md, cuatro rangos de
  # línea que apuntaban a la AC contigua, un registro inventado (api/.accepted-risk) y cinco recuentos
  # inflados. El artefacto pasa de 1009 a 1143 líneas y de 26 a 27 AC.
  # img-1-3: Task 0 RESUELTA 2026-08-30 — la señal de fallo de lectura se deja SIN ACOTAR (opción D), tras dos
  # rondas de consulta externa y tres mediciones. La ronda 1 recomendó D condicionada a que el canal fuera una
  # alarma; medido, NO lo es (stream plano en los tres entornos, handler de Sentry comentado y excluyéndolo,
  # cero colectores en compose), así que la condición es falsa. D sobrevive por otra razón: las cotas
  # disponibles están en el punto de control equivocado, porque un 5xx escribe ADEMÁS un `error` en el canal
  # por defecto (ExceptionResponder.php:293) que activa el handler bufferizado y es inalcanzable desde el
  # módulo. La ronda 2 corrigió mi argumento (rechaza A/B/C, no prueba D por sí solo) y aportó la gobernanza:
  # un requisito escrito no se reinterpreta desde la historia que lo incumple. Resultado: ADR nuevo
  # (docs/adr/image-read-failure-signal-bound.md, con las cuatro alternativas descartadas una a una, incluido
  # que el cap por petición es un no-op MEDIDO), AC del épico enmendada apuntando al ADR y conservando su
  # redacción original para que el cambio sea auditable, AC 9 pasa de «acota» a «declara con la medición»,
  # AC 22 gana un quinto residual reencuadrado como SHARED-SINK EVICTION entre productores independientes, y
  # la medición queda acotada a coste POR EVENTO declarando que no mide frecuencia ni producción (no hay
  # despliegue de producción). Cero código. Issue de infraestructura abierta (#879): el aislamiento del sumidero
  # es transversal a todos los contextos y no de esta épica.
  # img-1-1: code review (bmad-code-review, PR #862) completo 2026-08-27 — 1 decision-needed
  # resuelto (fail-closed) y 6 patch aplicados, make php.stan/php.quality/php.unit en verde.
  # img-1-2: code review (bmad-code-review, PR #873) completo 2026-08-29 — tres capas en paralelo
  # sobre el código ya endurecido por los tres pases previos: 1 decision-needed y 19 patch, TODOS
  # aplicados en la misma PR, más un defer refundido en img-1-3 (la ausencia confirmada es un
  # productor de log sin cota que sólo se vuelve alcanzable cuando 1.3 exponga `read()`). El
  # hallazgo que ningún pase anterior vio: la storage key se derivaba de la GRAFÍA del `ImageId`
  # mientras la fila se selecciona por su VALOR, así que un id en mayúsculas informaba una erasure
  # confirmada, borraba la fila y dejaba los bytes sin referencia para siempre. Se cierra
  # normalizando en el constructor del VO. Decision-needed: el bus entero corría con el default de
  # Symfony — siete segundos de reintento — así que `async` declara ya su `retry_strategy`.
  # Gates frescos: php.quality.dry-run 0 · php.unit 0 (3333 tests) · php.md 0 · shellcheck 0.
  # img-1-3: las seis decisiones abiertas quedan CERRADAS 2026-08-30 (D1 Infrastructure/Controller/ — medido,
  # en Backoffice Http/ es sólo para mappers · D2 aceptar must-revalidate · D3 id por escenario · D4 step
  # genérico · D5 CERO issues nuevas · D6 heredar not-found). Feedback de Sergio aplicado: el default es
  # arreglar en la PR, no abrir issue — escrito como regla en CLAUDE.md y aplicado aquí, donde el residual
  # del objeto en memoria se cerró con una guarda en la AC 7 en vez de con una issue.
  # img-1-3: max-age baja de un año a 3600 s (2026-08-30), supersediendo el valor del épico. La inmutabilidad
  # del IDENTIFICADOR no implica cacheabilidad indefinida de los BYTES: el módulo no reemplaza nunca una
  # representación, pero su contrato es el borrado fiable y el borrado es otro evento de ciclo de vida. 3600
  # queda etiquetado como PRIOR, no medición (no hay SLA ni consumidor); si el SLA fuera cero la respuesta
  # sería no-store. Sin ADR: aquí se hace MÁS que el requisito, no menos.
  epic-images: done
  img-1-1-subir-imagen-obtener-representacion-canonica: done
  img-1-2-persistir-imagen-borrado-fiable-de-bytes: done
  # img-1-3: code review de tarea completa hecha 2026-08-31 — tres capas en paralelo sobre el código, 17
  # hallazgos nuevos distintos más los 18 del Pase 3, todos aplicados salvo uno descartado por estar ya
  # registrado. Cuatro decisiones resueltas con Sergio tras consulta externa; la ADR del bound de la señal
  # queda ENMENDADA y su D2 revertida. El marcador pasa a `done` porque esa review es el último paso que
  # lee la tarea entera; NO es evidencia de que la rama haya mergeado — para eso, git.
  img-1-3-leer-representacion-canonica-de-forma-segura: done
  epic-images-retrospective: done

# Los action items de la retrospectiva de esta épica NO viven aquí: viven en el `action_items:` del
# `sprint-status.yaml` canónico, junto a los de gdpr-hardening. La razón NO es el audit — su parser sólo
# lee pares `key: value` a dos espacios bajo `development_status:` y nunca ha leído `action_items`. Es
# `bmad-sprint-status`, que resuelve el canónico por convención fija de skill; y que tenerlos en dos
# sitios es tenerlos derivando.
# Retrospectiva: `epic-images-retro-2026-08-31.md`.
