# Deferred Work

## Deferred from: code review of spec-shared-prod-hardening (2026-05-31)

- **Spec Task 3 / Code Map vs reality** — Task 3 and the Code Map claim a `compose.yaml` change ("add named networks", neutralize fallbacks), but all secret-neutralization (`${VAR:?}`) and the `frontend`/`backend` (internal) networks landed only in `compose.prod.yaml`. The base file was untouched. Outcome is correct and cleaner; the spec text is inaccurate. Spec-hygiene fix only.
- **Uncatalogued `api/` edits** — `api/frankenphp/docker-entrypoint.sh` received a real functional change (guards the composer-reconcile block behind `command -v composer` so the slim prod image, which ships no composer, doesn't exit-127 boot-loop). Verified correct: dev behavior unchanged, prod boot fixed; arguably necessary for AC1. It is not in the spec's Code Map/Tasks — acknowledge as intentional scope in the PR description. (`api/config/reference.php` is handled as a patch — revert.)
- **`trust-local.sh` assumes `runuser` present** — on a minimal/busybox or non-PAM host without `runuser`, the NSS import calls fail under `set -e`, aborting mid-trust after `/etc/hosts` and the system CA were already modified. Guard with `command -v runuser` + graceful skip. Narrow host edge case.
- **`CADDY_SERVER_EXTRA_DIRECTIVES` "delete line" robustness** — the single-dash default `${CADDY_SERVER_EXTRA_DIRECTIVES-tls internal}` is intentionally correct (an explicitly-empty value yields ACME), but an operator who *deletes* the line entirely (vs setting it empty) silently re-enables `tls internal` on a public VPS, breaking ACME. The VPS doc says "clear/empty" (consistent), but add a guard comment in `.env.prod.example` for robustness. (`compose.prod.yaml:500`)
- **Smoke-test retry window (~30s)** — `deploy-local.sh` retries the health curl 6×5s. `make docker.up.wait --wait` already gates on container health before the smoke test, so this is likely sufficient; extend the retries only if cold prod boots are observed to exceed the window. (`scripts/deploy/deploy-local.sh:110-115`)
