# generated: 2026-06-23
# last_updated: 2026-07-30
# project: ERPify
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-06-23
last_updated: 2026-07-30
project: ERPify
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Tracks the REGULATORY audit trail: docs/adr/regulatory-audit-trail.md +
  # _bmad-output/planning-artifacts/epics-regulatory-audit-trail.md (E1 pre-auth · E2 gated-by-data · E3 gated-auth).
  # It supersedes the tracking of the operational/actor axis (audit-activity-log.md), whose epics
  # shipped (PRs up to #402); that axis's record lives in git history and its PRs, not here.

  # Epic 1 (E1): Captura de escrituras regulatoria — diff campo a campo, pre-auth (#394/#398/#399/#402)
  epic-1: done
  1-1-listener-doctrine-onflush-captura-changeset: done
  1-2-accion-semantica-forma-del-diff-en-metadata: done
  1-3-actor-context-correlacion-sellados-en-onflush: done
  1-4-snapshot-de-la-entidad-en-delete: done
  1-5-extractor-de-recurso-en-lecturas: done
  1-6-suelo-de-retencion-5-anos: done
  1-7-render-del-diff-en-el-read-model-ui-377: done
  epic-1-retrospective: done

  # Epic 2 (E2): Diffs PII-safe — crypto-shredding + keystore (disparada por el dato, no por un agregado)
  epic-2: done
  2-1-clasificacion-pii-por-campo-personaldata: done
  2-2-keystore-envelope-libsodium-dek-por-encryptionscopeid: done
  2-3-auditar-bankaccount-con-diff-pii-crypto-shredded: done
  2-4-erase-subject-olvido-por-sujeto-destruir-la-dek: done
  epic-2-retrospective: done

  # Epic auth-foundation (AF): Fundación auth/RBAC — prerequisite de E3, gated on nada (greenfield)
  # docs/adr/auth-rbac-subsystem.md + _bmad-output/planning-artifacts/epics-auth-foundation.md. DAG: AF → 3.1 → 3.2 → 3.3
  epic-auth-foundation: done
  af-1-1-user-aggregate-backoffice-identity-persistencia: done
  af-1-2-firewall-sesion-securityuser-authenticator-csrf: done
  af-1-3-baseline-control-acceso-default-deny-401: done
  # AF-1.4 = endurecimiento auth (PR #433: login-CSRF Origin-check + CLI SensitiveParameter). El residual
  # de hardening está trazado como issues abiertos: #435 (roles/password_hash corruptos → 500 en hot-path
  # auth), #436 (Email NFC/Unicode), #438 (deptrac no aísla Symfony Security). Endurecimiento futuro SIN
  # issue (opcional, no bloquea): timing dummy-hash, login_throttling, password policy, PasswordUpgrader.
  af-1-4-endurecimiento-auth-follow-ups: done
  epic-auth-foundation-retrospective: done

  # Epic 3 (E3): Acceso restringido + auto-auditado + atribución real (gated on auth-foundation)
  epic-3: done
  3-1-voter-rbac-rutas-lectura-trail-atribucion-real: done
  3-2-auto-auditoria-del-acceso-auditar-al-auditor: done
  3-3-levantar-el-gate-de-produccion: done
  epic-3-retrospective: done

  # Epic rbac-authorization-model (RM): modelo RBAC transversal Permission=(resource,action).
  # docs/adr/rbac-authorization-model.md (D1–D9) + _bmad-output/planning-artifacts/epics-rbac-authorization-model.md.
  # Extiende el subsistema auth/RBAC (SI-1…SI-9); no revoca el hermano. #437 co-requisito de RM-2/RM-4.
  # Orden de merge safe-first (aditivo → comportamiento): RM-1 → RM-2 → RM-5 → RM-3 → RM-4; RM-6 tras RM-1.
  # Las historias van en ese orden de merge (no en RM-N numérico): «siguiente backlog» de arriba-abajo es
  # dependency-safe. RM-4 tiene gate DURO en RM-2 — no mergea sin RM-2 en main (par nested/colección divergente).
  epic-rbac-authorization-model: done
  rm-1-nucleo-autorizacion-voter-puerto-politica: done
  rm-2-cierre-fingerprint-keyset-437: done
  rm-5-migracion-audit-audittrail-read: done
  rm-3-gateo-rutas-bank-tightening: done
  rm-4-gateo-bankaccount-coleccion-anidada: done
  rm-6-gate-ocp-congelar-core: done
  epic-rbac-authorization-model-retrospective: done

  # Epic identity-invitation-lifecycle (II): ciclo de vida de identidad/invitación — onboarding invitation-first,
  # admisión de tres momentos (credenciales → identidad → admisión → sesión), registro de sesiones fail-closed.
  # docs/adr/identity-invitation-lifecycle.md (D1–D12) + _bmad-output/planning-artifacts/{arch-addendum-identity-
  # invitation.md, epics-identity-invitation-lifecycle.md}. Extiende auth/RBAC (SI-1…SI-9) sin revocarlo; ortogonal
  # al plano RBAC. Multi-tenant-ready, tenancy operativa diferida. Orden de merge safe-first (las historias van en
  # ese orden, no en II-N numérico): II-0 → II-1·II-2·II-3·II-7 → II-6 → II-4 → II-5 → II-8. II-0 co-mueve Identity
  # + core RBAC (opción b; RM-1 ya en main vía #456). II-4/II-5 exigen II-7 (registro/revoca-todas).
  epic-identity-invitation-lifecycle: done
  ii-0-promocion-contexto-iam-organization-comove-rbac: done
  ii-1-organization-membership-bootstrap-cli: done
  ii-2-shared-token-singleusetoken: done
  ii-3-identitystatus-userchecker-admision-tipos-error: done
  ii-7-session-lifecycle-registry-gate-failclosed: done
  ii-6-lockout-lockeduntil: done
  ii-4-invitation-accept-pantallas-acceso: done
  ii-5-reset-password-uniforme: done
  ii-8-endurecimiento-seguridad-transversal: done
  epic-identity-invitation-lifecycle-retrospective: done

  # Epic users-admin (U): Administración de usuarios (back-office/Iam) — conectar la consola PWA /backoffice/users
  # (hoy mock CrudRepository) al backend real Iam/Identity (invitation-based). arch-addendum-users-admin.md (SI-16…20)
  # + epics-users-admin.md. PR #500. Orden de merge safe-first (las historias van en ese orden, no en U-N numérico):
  # U-0 → U-1 → (U-2 · U-3) → U-4 · [U-5a → U-5b]. U-5b (borrado GDPR) tiene gate DURO en U-5a (cerrar #376).
  # U-4 (edición de roles) es candidato. erase gana UI (SI-19 enmendado). Cada historia = su propia rama (gate de ramas).
  epic-users-admin: done
  u-0-read-side-lista-detalle-conectados-backend: done
  u-1-me-deriva-permisos-gateo-can: done
  u-2-invitar-alta-invitacion: done
  u-3-cambio-de-estado-suspend-deactivate: done
  u-4-edicion-de-roles-candidato: done
  u-5a-cerrar-376-tombstone-actor-ids: done
  u-5b-borrado-gdpr-consola: done
  epic-users-admin-retrospective: done

  # Epic gdpr-hardening (G): garantías de borrado ejecutables — convertir las garantías de borrado de datos
  # personales de prosa en mecanismo con control capaz de fallar, sobre Iam/Identity, Iam/Invitation,
  # Organization/Membership, Shared/Audit y los transportes Messenger persistidos.
  # arch-addendum-gdpr-hardening.md (SI-21…SI-23, #606/#607) + epics-gdpr-hardening.md (PR #609).
  # No introduce principio nuevo: extiende el patrón ya institucionalizado (registro declarativo + gate
  # obligatorio, 6 instancias). Unidad = EJE de garantía, no issue: cada historia instala declaración +
  # mecanismo + control capaz de fallar. Orden de merge safe-first (las historias van en ese orden, no en
  # G-N numérico): G-4a → G-1a → (G-1b · G-2) → G-3a · G-3b. G-1b y G-2 tienen gate en G-1a (el eje, que
  # llega VERDE); G-4a, G-3a y G-3b son independientes y paralelizables. G-4a primero por ser la única fuga
  # viva medida — y es la primera instancia de SI-21, no una excepción a él.
  # G-3a y G-3b arrancan con una DECISIÓN de diseño abierta (qué cuenta como segundo testigo · dónde vive el
  # schedule de Iam). Precondición normativa de la épica: ninguna implementación empieza antes de que esa
  # decisión quede registrada por escrito; con recomendación, la primera tarea es confirmarla o refutarla.
  # Definición de hecho de la épica: ninguna historia llega a done sin pase adversarial REGISTRADO por alguien
  # distinto del autor, declarando dónde quedó (CLAUDE.md → Security review → Process).
  # FUERA: G-4b (eje Messenger completo) diferida, bloqueada por el ownership de referencias nacidas en
  # configuración — no es GDPR sino metadatos arquitectónicos. #567 (¿sobrevive el CLI audit:gdpr:erase?)
  # decisión pendiente del usuario, ninguna historia lo presupone. #562/#565 seguimiento operativo. #564 fuera
  # por ALCANZABILIDAD (php no declara replicas: en compose.prod.yaml); tripwire de reapertura: réplicas de php
  # o deploy rolling, momento en el que pasa a defecto de DISPONIBILIDAD urgente.
  epic-gdpr-hardening: in-progress
  g-4a-fuga-passwordresetcompleted-transportes-messenger: done
  # G-1a arranca con TRES decisiones abiertas que el corte no marcó porque nacen de mediciones que el corte no
  # hizo: (1) ¿el gate DETECTA huecos o solo verifica lo declarado? — "llega verde" y "cada hueco posterior es un
  # gate rojo" NO son simultáneamente ciertas; (2) dónde vive el generador y cómo escribe (sin precedente en el
  # repo, y el camino obvio choca con el read-only/parallel-safe que php.quality.dry-run declara); (3) forma de la
  # clave. Precondición normativa de la épica: nada se implementa antes de registrarlas por escrito.
  g-1a-eje-declaracion-atributo-registro-generado-gate: ready-for-dev
  g-1b-referencias-huerfanas-membership-invitation: backlog
  g-2-ids-de-persona-fuera-de-audit-log-metadata: backlog
  g-3a-segundo-testigo-del-registro: backlog
  g-3b-schedule-observable-alertado-del-reconciliador: backlog
  # G-5 (FR10) NO estaba en el corte original: se descubrió midiendo al contextualizar G-4a. El `event_store`
  # conserva el id real del sujeto a perpetuidad (aggregate_id de 6 eventos + payload de SessionStarted y los
  # seis Invitation*), ningún routing lo cambia y la cadena de erasure no toca la tabla — luego G-4a persigue la
  # fuga MENOR. No se abrió issue (decisión de Sergio, 2026-07-30: el contador ya está saturado); vive aquí y en
  # epics-gdpr-hardening.md. Bloqueada por una decisión de ESTRATEGIA DE PERSISTENCIA que es del usuario y va a
  # ADR: crypto-shredding no aplica a una columna clave e indexada, y D4 veta la tabla de correspondencia.
  # Mientras G-5 no cierre, la épica NO puede declararse completada.
  g-5-ids-de-persona-fuera-del-event-store: backlog
  epic-gdpr-hardening-retrospective: optional

# Action items — retrospectiva users-admin (2026-07-21). Principio: resolver, no acumular.
# Detalle y triaje en epic-users-admin-retro-2026-07-21.md.
# NOTA: 5 items resultaron deuda FANTASMA / ya-resuelta (verificado en main) y se removieron: FetchHttpClient
# try/catch (#520), 500 array-claves en invitación (#509), InvitedIdentityUnavailable markerless-500 (#507), y
# las "5 páginas de detalle" (la nota de review de U-4 era falsa — ya distinguen 404 vía ViewStatus.NOT_FOUND).
# Resueltos tras la retro y removidos: a11y status:0 (#531), aserciones de status.feature (#532).
# NOTA (2026-07-22, verificado contra main, no contra esta foto): removidos otros 3.
#   · realtime/authorize — DECIDIDO y shipped en #535: se descartó el 204-sin-cookie (borraría la señal de
#     seguridad que audita AccessDeniedAuditListener y que Behat pinnea) y se implementó la degradación PWA:
#     401/403 son terminales y cierran la suscripción. Residual → deferred-work.md (feed no se re-arma).
#   · fetch-antes-del-gate — RESUELTO por otra vía en #539: el flag `enabled` en los hooks compartidos se
#     descartó (Regla de Tres incumplida, 2 consumidores) y el gate <Can> subió por encima del hook mediante
#     cáscaras page.tsx + _components/. Residual → deferred-work.md (gatear el menú lateral).
#   · transporte failed — REPLANTEADO como #546: medido en main NO hay fuga viva (SendEmailMessage sin enrutar,
#     BankAccountSnapshot excluye PII). El gap real es que nada EJECUTA esa invariante. La pasada adversarial a
#     U-5b abrió además #545 (membership huérfana) y cerró #495 (sesiones, ya resuelto por #529); la regla de
#     proceso quedó fijada en CLAUDE.md → "Security review on every change" → Process.
# DECIDIDAS 2026-07-22 (round trip con IA externa, contrastado contra el repo) y removidas de aquí:
#   · SI-15 autoridad de roles → A1, User.roles única fuente; se elimina Membership.roles → #549.
#     Descartadas A2 (obligaría a cruzar frontera en auth y en el guard ≥1-admin) y A3 (consistencia
#     eventual en la ruta de autorización). Condición de reapertura escrita en el issue.
#   · Core RBAC → NO se mueve. La premisa "shared kernel de facto" es falsa: cero imports de tipo fuera
#     de Iam/, el acoplamiento es por literal de cadena en #[IsGranted], y Role ya vive en Shared/Access.
#   · Orden: #545 (seam de erasure, arregla el defecto) ANTES que #549 (limpieza de modelo). Quitar
#     Membership.roles NO cierra #545 — el residuo GDPR es el user_id, no los roles.
#   · Se descartó abrir un ADR sobre si Membership sigue siendo agregado: su organization_id se lee en
#     hot path de login y el seam de #545 le devuelve comportamiento, así que la forma final emerge de
#     esos dos cambios en vez de deliberarse antes.
# DECIDIDAS 2026-07-29 (pase de tres personas BMAD: arquitectura / implementación / test-riesgo, medido
# contra main) y removidas de aquí — con ellas el registro queda vacío:
#   · Política de rol del invitador (#505) → permiso `users.grantAdmin` declarado CON fila `=> [ADMIN]`,
#     chequeado en los dos controllers sólo cuando el payload trae ADMIN, más la fila de auditoría con actor
#     (ya escrita y revertida en 50ed6708 esperando #555, que cerró en #558). Alcance ampliado: superficie
#     HTTP para RevokeInvitation — hoy una invitación ADMIN vive 3 días y sólo se revoca por CLI. La fila
#     nace en [ADMIN] y no vacía porque `users.invite` y `users.changeRoles` son ambos ADMIN-only: prohibir
#     que un ADMIN acuñe ADMIN dejaría al producto sin forma de crear un segundo administrador, y eso hace
#     INSATISFACIBLE el borrado GDPR del admin único (FulfilIdentityErasure rechaza con 409 a quien tiene
#     ADMIN). Restringir más es una edición de una línea de datos. Descartados: rung OWNER, matriz de
#     delegación, type-to-confirm como control, y pasar los roles al voter como subject. Orden: tras #549.
#     El ADR authorization-model-boundaries.md necesita enmienda — su frase del "no-op" es cierta del
#     ADMIN-superusuario anterior y falsa desde #550 (TIER_OPT_OUT incluye 'users').
#   · Lockout por email = DoS contra admins → ACEPTADO tal cual, registrado en #602, y condicionado a fijar
#     antes el test de que una sesión viva sobrevive al bloqueo (el muro salta en UserChecker::checkPostAuth,
#     no en SessionAdmissionGate) — es la premisa de la que cuelga toda la evaluación y nada la pinnea.
#     Descartados: dimensión IP/origen (re-deriva login_throttling y abandona el caso del spray distribuido
#     que el contador existe para cazar), y MFA como respuesta a esto (el muro es anterior al segundo factor).
action_items: []
