# Arch addendum — GDPR hardening: garantías de borrado ejecutables (scoped)

> **Estado:** propuesto — épica sin cortar · **se apoya en** el rastro regulatorio ([`../../docs/adr/regulatory-audit-trail.md`](../../docs/adr/regulatory-audit-trail.md)) y el eje operativo ([`../../docs/adr/audit-activity-log.md`](../../docs/adr/audit-activity-log.md)) · **extiende** el subsistema de identidad ([`arch-addendum-identity-invitation.md`](arch-addendum-identity-invitation.md)) y la consola de usuarios ([`arch-addendum-users-admin.md`](arch-addendum-users-admin.md)) sin revocarlos. **Alcance:** las garantías de borrado de datos personales sobre `Iam/Identity`, `Iam/Invitation`, `Organization/Membership`, `Shared/Audit` y los transportes Messenger persistidos. **No** construye superficie de cumplimiento (el mapa dato-personal → evidencia es un entregable posterior, cuando documente controles existentes en vez de intenciones), ni tenancy, ni base jurídica ni plazos de conservación fuera del suelo de 5 años ya vigente.
> **Decisiones (el *qué* y el *por qué*):** el eje de la épica está **cerrado** (mesa con IA externa en dos vueltas, contrastada contra el repo); el histórico argumental vive en `tmp/bmad-md/consult-gdpr-hardening-epic-axis-20260729-002631.md` y el registro de decisiones y mediciones en `architecture/gdpr-hardening/.memlog.md`.
> **Jerarquía:** `epics.md` **>** este addendum. Extiende SI-1…SI-20 sin revocarlos.
> **methodology:** contract-first scoped sobre subsistema maduro — invariantes globales mínimos + localización por historia + DAG; se omite el march de 8 pasos. Precedente de método: [`arch-addendum-users-admin.md`](arch-addendum-users-admin.md).

La épica **no introduce un principio arquitectónico nuevo**. Extiende un patrón que el repo ya institucionalizó —regla declarativa + gate obligatorio, con seis instancias medidas (`php.lint.audit-resource`, `.bounded-context`, `.error-contract`, `.event-bus`, `.doctrine`, `php.deptrac`)— al dominio de las garantías de borrado. Cada historia instala un **eje de garantía completo**: declaración de propiedad, mecanismo de ejecución, y control capaz de fallar.

## System Invariants (globales — los que introduce esta épica)

Continúan la numeración del subsistema (SI-1…SI-20); estos tres son los que introduce.

- **SI-21 · Toda referencia persistida a una persona física tiene un dueño de borrado declarado, y la declaración está verificada.** El dueño se declara en un artefacto **revisable**; un mecanismo automático verifica que toda declaración **existe y está correctamente cableada**. El mecanismo por defecto **rompe el build**; un control agendado solo se admite cuando la propiedad **no puede decidirse estáticamente**, y en ese caso el agendado, el fallo observable y el alertado forman parte de **la misma entrega**. La división es la que ya practica [`../../api/.audit-resource-types`](../../api/.audit-resource-types): **el humano clasifica, la automatización verifica** — el gate nunca juzga la clasificación, solo que exista y esté conectada. *Binds:* toda historia de la épica. *Prevents:* que una garantía de borrado viva solo en prosa o docblock; y que un control detective exista sin que nadie lo ejecute. *Alcance de «referencia»:* incluye el `aggregateId` de un evento persistido cuyo agregado **es** una persona — un payload «solo el id del agregado» no es seguro por construcción, lo es **si y solo si el agregado no es una persona**. *SI-21 no prescribe el mecanismo de declaración:* la forma depende del **origen** de la referencia (modelo de dominio, constante, atributo, configuración), y forzarlos todos al mismo mecanismo sería un error — lo que SI-22 acota es la derivabilidad, no la uniformidad.
- **SI-22 · Lo derivable se genera; lo manual se justifica y se atestigua.** Toda declaración **derivable del código se genera automáticamente** y el gate falla cuando lo generado y lo commiteado divergen. Toda declaración **manual** se justifica por no ser derivable — y exige un **segundo testigo** independiente de la propia declaración. *Binds:* todo registro declarativo que la épica cree o repare. *Prevents:* el ciclo lógico de #563, donde el registro es a la vez fuente, evidencia y objeto comprobado. *Por qué funciona:* una entrada **generada** no puede satisfacerse a sí misma —su contenido es una huella del código, no una afirmación—; una **manual** sí puede, y por eso necesita el segundo testigo. *Medido:* la generación total es imposible aquí — un tipo nace de un `defaults:` de ruta, de una constante de clase y de 8 entradas de routing en YAML, ninguna alcanzable por reflexión sobre propiedades. La arquitectura es **híbrida por fuerza**, no por comodidad.
- **SI-23 · Una declaración nunca es su propia evidencia.** Un control cuya evidencia es la declaración que debería verificar no es un control: lee verde por construcción. *Binds:* todo registro, gate o control del repo, no solo los de esta épica. *Prevents:* la familia entera de #563. *Es el principio que SI-21 y SI-22 instancian* — SI-21 lo aplica separando quién clasifica de quién verifica; SI-22 lo aplica prefiriendo la huella generada sobre la afirmación escrita. *(Principio transversal; **merece ADR propio** — sobrevive a esta épica y debería consultarse cada vez que aparezca un registro declarativo nuevo.)*

## Invariantes heredados (consumidos, no re-derivados)

- **D4** ([`../../docs/adr/audit-activity-log.md`](../../docs/adr/audit-activity-log.md)) — prohibida toda tabla crosswalk `id real → pseudónimo`. Ninguna historia la introduce, en columna ni en JSONB.
- **D15** ([`../../docs/adr/regulatory-audit-trail.md`](../../docs/adr/regulatory-audit-trail.md)) — `erase-actor` y `erase-subject` son operaciones legales **distintas** y no se fusionan nunca; descartado extender `erase-actor` para que además haga trabajo de subject. **Matiz medido:** D15 descarta *extender*, no prohíbe que `erase-actor` se **niegue** a correr — una guarda no fusiona las dos operaciones.
- **SI-19** — el erase des-identifica identidad + rastro como **una unidad**; un erase que solo borre la identidad es incompleto.
- **Aislamiento de contextos** — `Shared/Audit` no aprende semántica ajena. **Matiz medido:** `Shared/Audit` posee la *forma* `(type, id)` de `AuditResource`, **no el vocabulario** — cada contexto mintea su propio string de tipo. El Shared Kernel evita dependencias de compilación, no de significado.
- **Crypto-shredding** — cubre **solo `audit_log`** (`PiiDiffSealer` + destrucción de DEK por `EncryptionScopeId`). No cubre payloads de Messenger ni ninguna otra tabla.
- **`#[PersonalData]`** ([`../../api/src/Shared/Privacy/Domain/PersonalData.php`](../../api/src/Shared/Privacy/Domain/PersonalData.php)) — su contrato es de **tratamiento** («esta propiedad *contiene* dato personal» → el consumidor decide cifrar-vs-claro por columna), no de referencia. **No se reutiliza** para marcar referencias a persona: `PiiDiffSealer` consume `personalFieldsOf()`, y anotar una clave foránea haría que el sellador la cifrara en el diff, rompiendo las búsquedas que el propio reconciliador de erasure necesita.

## Localización de decisiones por historia

*Tabla de localización propuesta; el corte fino (numeración/AC) es `bmad-create-epics-and-stories`.*

| Historia | Invariantes / decisiones | Costura / artefactos que toca |
|----------|--------------------------|-------------------------------|
| **G-1a — el mecanismo (verde al llegar)** | SI-21, SI-22, SI-23 | Atributo **hermano** de `#[PersonalData]` en `Shared/Privacy` (no reutilización) que marca *«esta propiedad guarda el identificador de una persona»* + su dueño de borrado; clasificador por reflexión análogo a `ReflectionPersonalDataClassifier`; registro **generado** y commiteado; gate `make php.lint.person-reference` que falla si generado ≠ commiteado o si una referencia no declara dueño. **Llega verde** declarando las referencias que la cadena de erasure **ya** borra hoy (`PasswordResetToken::$userId`, `Session::$userId`) — el mecanismo se estrena sobre comportamiento correcto, y cada hueco posterior es un gate en rojo que su historia cierra. |
| **G-1b — cerrar las referencias huérfanas** | SI-21; consume ≥1-ADMIN | Con el gate en rojo: `Organization/Membership.user_id` (#545) e `Iam/Invitation.invited_user_id` (#561). **No son dos bugs sino dos manifestaciones del generador** — la arquitectura *obliga* a referenciar por id entre contextos, así que cada contexto nuevo produce otra. **Cuidado con #545:** el guard `keepsAnActiveAdminWithout` lee `identity_user`, **no** membership, así que una membership fantasma con rol `ADMIN` deja el invariante «≥1 ADMIN activo» leyendo satisfecho. Cerrar la referencia y el invariante van juntos. |
| **G-2 — `audit_log.metadata`** | SI-21 | `EraseIdentitySubject` escribe `'subject_user_id' => $userId` en el `metadata` de la fila `GDPR_SUBJECT_ERASED` ([`../../api/src/Iam/Identity/Application/EraseIdentitySubject.php`](../../api/src/Iam/Identity/Application/EraseIdentitySubject.php)), y ningún anonimizador toca `metadata`. Hoy **no** es crosswalk solo porque un comentario impide que un pseudónimo comparta esa fila — es decir, D4 está sostenido por prosa. Redactor de `metadata` en la misma pasada de erasure, **o** gate que prohíba ids de persona en claves de `metadata`. |
| **G-3 — reparar el gate y agendar el control** | SI-22, SI-23 | **#563:** el check de staleness se satisface con su propia declaración — el literal `'User'` aparece **una sola vez** en todo `api/src`, en la constante que la entrada del registro señala. Necesita el **segundo testigo**. **#566:** `ReconcileErasedSubjectReferences` no está en ningún schedule, y el listener de acceso emite en `kernel.terminate`, **después** del commit del erase → resurrección. **Decisión de diseño, no cableado:** los dos únicos schedules del repo viven bajo `Shared/*`; el reconciliador vive en `Iam/Identity`. No hay precedente de schedule de mantenimiento dentro de un contexto de negocio. |
| **G-4a — cerrar la fuga viva** | SI-21 | **Fuga medida, no registrada en #546:** `PasswordResetCompleted` está enrutado a `async` ([`../../api/config/packages/messenger.yaml`](../../api/config/packages/messenger.yaml)); su agregado es `User`, y su propio docblock dice *«the subject is the aggregate id alone»* — luego el `aggregateId` **es el id del usuario**. Se serializa a `messenger_messages` (sin TTL ni poda) y a `failed` (sumidero permanente), y `FulfilIdentityErasure` no toca ninguna tabla de Messenger. El razonamiento del comentario de routing es **correcto para Bank y falso para User**. Cerrar la fuga es barato y urgente; **no** requiere resolver la pregunta abierta de abajo. |
| **G-4b — el eje Messenger completo** | SI-21, SI-22 · **bloqueada** | Instalar dueño de borrado y gate sobre referencias que **nacen en configuración**. Bloqueada por la pregunta abierta de *ownership de metadatos arquitectónicos* (ver Riesgos). |

## DAG de dependencias

```
G-1a (mecanismo: atributo + registro generado + gate; VERDE al llegar)
  ├─> G-1b (#545 + #561; el gate se pone rojo y la historia lo cierra)
  └─> G-2  (metadata del audit_log, #560)

G-3  (segundo testigo de #563 + agendado de #566)        [independiente de G-1]

G-4a (cerrar la fuga de PasswordResetCompleted)          [independiente · URGENTE]

[PREGUNTA ABIERTA: ownership de referencias nacidas en configuración] ─> G-4b
```

**Orden safe-first:** `G-4a → G-1a → (G-1b · G-2) → G-3`. G-4a va primero pese a no ser el eje: es la única fuga **viva** medida, y cerrarla no depende de nada. G-1a es aditivo y llega verde. G-3 es independiente y puede paralelizarse. G-4b no entra en esta épica.

## Riesgos / notas de secuenciación

- **Pregunta abierta — ownership de referencias nacidas en configuración.** El routing de Messenger materializa una referencia persistida sin que ninguna propiedad de dominio la declare. La pregunta —*¿quién posee una referencia que no nace en el modelo sino en configuración?*— **no es de GDPR sino de ownership de metadatos arquitectónicos**, y su respuesta afectará también a scheduler, rutas, integraciones y eventos externos. Se deja **explícitamente abierta**: el addendum documenta decisiones tomadas, no es el sitio donde se resuelven las futuras. Merece su propia historia de diseño.
- **#567 — sin resolver, y no por lo que parecía.** No es *«cómo arreglamos el CLI»* sino *«¿debe existir un CLI que solo se usa con seguridad porque el operador leyó el `--help`?»*. Medido: `EraseActorAuditTrailCommand` **no tiene invocadores** en `api/src` — las únicas menciones son referencias cruzadas en docblocks. D15 **no** bloquea la guarda (descarta *extender*, no *negarse*); el obstáculo real es que la clasificación «`User` denota persona» es vocabulario del contrato de auditoría, no del modelo de dominio, así que moverla a `Shared/Privacy` sería inversión en la dirección contraria. YAGNI empuja a eliminar el comando antes que a hacerlo más listo. **Decisión pendiente del usuario.**
- **#564 — candidato, pendiente de medición.** Las migraciones añaden columna `NOT NULL DEFAULT` y quitan el default en la misma migración, y migran en boot de contenedor → durante un deploy multi-réplica fallan las escrituras de auditoría de las réplicas viejas. Si esas escrituras son best-effort (la interfaz `AuditLogWriter` es `write(): void`, sin contrato de error, y #526 describe el comportamiento), un deploy **pierde filas del rastro en silencio** — y eso rompe el suelo de retención de 5 años igual que una fuga. **Entra en la épica si se confirma leyendo los call-sites que consumen `AuditLogWriter`.** Sin esa medición es operativo.
- **Fuera de la épica: #562 y #565.** Ninguno cambia si una garantía *puede fallar*. #565 (deadlock entre erasures concurrentes) además falla seguro y es reintentable. Seguimiento operativo, no historia.
- **El segundo testigo de #563 puede no ser construible todavía.** Nada en `api/src` escribe hoy una fila con `resource_type = 'User'` — el gate está deliberadamente por delante de su productor. Si el segundo testigo se define como «existe un escritor real», la historia queda bloqueada por una funcionalidad que aún no existe. Alternativa a evaluar en el corte: un testigo de fixture o de escenario Behat, distinto de la declaración pero sin exigir un productor de producción.
- **La convergencia no es verificación.** El clúster #560–#567 salió de **una sola** pasada adversarial sobre #558, y la historia GDPR previa se mergeó autocertificada. Las cinco premisas se midieron contra `main` antes de escribir este documento y las cinco resultaron vivas — pero dos hipótesis adicionales se midieron y **una se refutó** (los eventos de sesión/invitación no están enrutados). Ninguna historia de esta épica llega a `done` sin pase adversarial registrado, por la regla de proceso de [`../../CLAUDE.md`](../../CLAUDE.md).

## Siguiente paso BMAD

Cortar la épica con `bmad-create-epics-and-stories` — **G-4a primero** (fuga viva), luego G-1a como mecanismo. Este addendum es su contrato de entrada. Antes de G-3, medir #564 para decidir si entra. El mapa de cumplimiento (`dato personal → contexto → responsable → mecanismo → evidencia`) es un entregable **posterior**, cuando documente controles construidos en vez de intenciones. SI-23 merece ADR propio, independiente de esta épica.
