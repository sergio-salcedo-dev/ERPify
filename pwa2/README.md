# `pwa/` — Next.js frontend

This directory holds the **Next.js** application for ERPify: the browser-facing UI that consumes the HTTP API under [`../api/`](../api/).

See the [root README](../README.md) for how the monorepo fits together.
