name: Docker setup
description: Checkout + Docker Buildx
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v6
      with:
        persist-credentials: false

    - uses: docker/setup-buildx-action@v4
