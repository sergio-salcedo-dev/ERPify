name: Full setup
description: Node + Docker
runs:
  using: "composite"
  steps:
    # Compose the single-purpose setups so each pinned action SHA lives in exactly
    # one place — full-setup is just node-setup + docker-setup, never a second copy.
    - uses: ./.github/actions/node-setup
    - uses: ./.github/actions/docker-setup
