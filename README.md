# ERPify

Bootstrap the API env file: `cp api/.env.example api/.env` (see `api/.env.example` for variables including `XDEBUG_MODE`).

**Host prerequisites** (Docker, Make, optional `jq` for `make health`): [docs/requirements.md](docs/requirements.md).