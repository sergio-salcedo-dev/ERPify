import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Commissions",
};

export default function CommissionsPage() {
  return <h1 className="text-foreground text-2xl font-semibold tracking-tight">Commissions</h1>;
}
