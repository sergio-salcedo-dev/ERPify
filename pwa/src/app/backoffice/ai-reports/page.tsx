import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AI Reports",
};

export default function AiReportsPage() {
  return <h1 className="text-foreground text-2xl font-semibold tracking-tight">AI Reports</h1>;
}
