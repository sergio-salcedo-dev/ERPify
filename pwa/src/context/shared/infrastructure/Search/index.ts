/**
 * Search serialization barrel. {@link buildSearchParams} turns the generic
 * `Filter[]` vocabulary into the API's `filters[N][…]` query string.
 */
export { buildSearchParams } from "./buildSearchParams";
