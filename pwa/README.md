# `pwa/` — Next.js frontend

This directory will hold the **Next.js** application for ERPify: the browser-facing UI that consumes the HTTP API under [`../api/`](../api/).

The project is not scaffolded here yet. When added, typical layout includes `package.json`, the Next.js app router or pages tree, and environment configuration pointing at the API base URL (local or deployed).

See the [root README](../README.md) for how the monorepo fits together.
